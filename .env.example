# Copy this file to `.env` and populate the values with your deployment details.
LLM_API_URL=http://localhost:8000/v1/generate
LLM_API_KEY=replace_with_your_api_key
# Optional: specify a default model identifier for your self-hosted LLM backend
LLM_MODEL=default-model
# Optional: override the endpoint used to discover available models or tags
LLM_MODELS_URL=http://localhost:8000/v1/models
